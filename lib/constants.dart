import 'package:flutter/material.dart';

// different color schemes for different widgets
const appbarColor = Colors.black;
Color? scaffoldBackgroundColor = Colors.white;
const reusableCardColor = appbarColor;
const reusableCardTextColor = Colors.white;
const reusableCardIconColor = Colors.tealAccent;
const reusableCardBackgroundColor = Colors.black;
const activeReusableCardColor = Color.fromARGB(255, 81, 79, 79);

// for slider
const numOfDivisions = 5;
const maxValueOnSlider = 5;
const activeSliderCol = Colors.tealAccent;
const inactiveSliderCol = Colors.grey;
const sliderTextCol = Colors.white;

// for AddMinusButton (num of sets and num of reps)
const iconColor = Colors.tealAccent;
const iconTextColor = Colors.white;